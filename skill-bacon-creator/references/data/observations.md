# Observations — skill-bacon-creator

Raw tool call outputs from Phase 2 — Observation Lab.
This is the primary evidence base. Every rule in `references/rules.md` must trace back to an entry here.

> **Completion rule:** ≥2 observations required to pass the Phase 2 gate.
> At least one must be a happy path. At least one must be an edge case or failure
> (HTTP 429 / 401 / 403 / 404 / empty / null / timeout).
> An observation recorded from memory or assumption is not an observation — it is a guess.

---

## Checklist

- [ ] ≥2 observations recorded
- [ ] ≥1 happy path (working call, valid response shape)
- [ ] ≥1 edge case or failure path (auth wall, rate limit, empty result, error)
- [ ] Each observation has a tool, query, raw result, and signal
- [ ] Idol flags recorded inline where bias surfaced
- [ ] `state.json` tool call counter updated after each call

---

## Observation 1 — [tool] — [ISO timestamp]
**Query:** (paste exact query, URL, or command)
**Result:**
```
(paste raw output here — truncate at 500 chars if needed, mark with …[truncated])
```
**Idols flagged:** (Tribe / Cave / Marketplace / Theatre — or "none")
**Signal:** (what this observation tells us about the skill's environment)

---

## Observation 2 — [tool] — [ISO timestamp]
**Query:** (paste exact query, URL, or command)
**Result:**
```
(paste raw output here — truncate at 500 chars if needed)
```
**Idols flagged:** (Tribe / Cave / Marketplace / Theatre — or "none")
**Signal:** (what this observation tells us)

---

<!-- Add more blocks as needed. Copy the block above. Max tool calls: 3× search_web, 1× exec, 1× fetch_url -->

---

## Example Observations (replace with real session data)

### Observation 1 — fetch_url — 2025-04-24T10:12:00Z
**Query:** `GET https://api.spotify.com/v1/search?q=jazz&type=playlist`
**Result:**
```
HTTP 401 Unauthorized
{"error":{"status":401,"message":"No token provided"}}
```
**Idols flagged:** Tribe — "Spotify search is public" assumed without verification. PATCHED: /search requires OAuth token.
**Signal:** Auth wall confirmed on first unauthenticated call. Rule needed: if 401, fallback to search_web.

---

### Observation 2 — search_web — 2025-04-24T10:14:00Z
**Query:** `Spotify API playlist search endpoint authentication requirements 2024`
**Result:**
```
Spotify Web API requires OAuth 2.0 for all endpoints including /search.
Client credentials flow supported for non-user data. Token endpoint:
https://accounts.spotify.com/api/token …[truncated]
```
**Idols flagged:** Theatre — "Spotify docs are current" — cross-checked against live 401 response. Consistent.
**Signal:** Client credentials flow available — no user login needed for playlist search. Simplifies auth.

---

> ⚠ The examples above are illustrations. Delete them and replace with your real session data.

---

*Generated by skill-bacon-creator v1.1 — Bacon's Empiricist Skill Builder*
