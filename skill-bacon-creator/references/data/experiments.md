# Experiments — skill-bacon-creator

Variant test logs from Phase 3 — Experiment Run.
Each entry records what was tested, what was expected, and what actually happened.

> **Completion rule:** ≥2 experiments required to pass the Phase 3 gate.
> Pass or fail both count — a failed experiment is real data.
> If all variants fail, write the No-Signal Declaration below instead of leaving this file empty.

---

## Checklist

- [ ] ≥2 experiments recorded
- [ ] Each experiment has a hypothesis, command, result, and rule implication
- [ ] No experiment result was assumed or invented — all outputs are real
- [ ] If all variants failed: No-Signal Declaration written below
- [ ] `state.json` tool call counter updated after each call

---

## No-Signal Declaration (fill in if applicable)

> **No-signal declared.** Experiment phase produced no positive signal.
> All variants failed or returned no usable data.
> Induction (Phase 4) will be limited to failure-mode rules only.
> Skill Foundry will include a `## Known Gaps` section in the output SKILL.md.

_(Delete this block if at least one experiment produced usable signal.)_

---

## Experiment 1 — [variant name] — [ISO timestamp]
**Hypothesis:** (what you expected to happen)
**Command / call:** (exact input, URL, or command)
**Result:** pass | fail | partial
**Raw output:**
```
(paste raw output here — truncate at 500 chars, mark with …[truncated])
```
**What this changes:** (rule implication — what if-then rule does this support or invalidate?)

---

## Experiment 2 — [variant name] — [ISO timestamp]
**Hypothesis:** (what you expected to happen)
**Command / call:** (exact input, URL, or command)
**Result:** pass | fail | partial
**Raw output:**
```
(paste raw output here — truncate at 500 chars)
```
**What this changes:** (rule implication)

---

<!-- Add more blocks as needed. Copy the block above. Stay within tool call budget. -->

---

## Example Experiments (replace with real session data)

### Experiment 1 — with_auth_header — 2025-04-24T10:18:00Z
**Hypothesis:** Adding a valid Bearer token will return a 200 with playlist data.
**Command / call:** `GET https://api.spotify.com/v1/search?q=jazz&type=playlist` + `Authorization: Bearer <token>`
**Result:** pass
**Raw output:**
```
HTTP 200 OK
{"playlists":{"items":[{"id":"37i9dQ...","name":"Jazz Vibes","tracks":{"total":80}}],"total":142}} …[truncated]
```
**What this changes:** Confirms Rule 1 happy path — authenticated call returns expected shape. Fields: id, name, tracks.total confirmed present.

---

### Experiment 2 — without_auth_header — 2025-04-24T10:19:00Z
**Hypothesis:** Call without token will return 401 — fallback to search_web should trigger.
**Command / call:** `GET https://api.spotify.com/v1/search?q=jazz&type=playlist` (no header)
**Result:** pass
**Raw output:**
```
HTTP 401 Unauthorized
{"error":{"status":401,"message":"No token provided"}}
```
**What this changes:** Confirms Rule 1 fallback — 401 is consistent and structured. Fallback to search_web is the correct response. Auth wall is not intermittent.

---

> ⚠ The examples above are illustrations. Delete them and replace with your real session data.

---

*Generated by skill-bacon-creator v1.1 — Bacon's Empiricist Skill Builder*
