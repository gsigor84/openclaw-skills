# Idols Log — skill-bacon-creator

Bias audit conducted at Phase 5 — Idols Checkpoint.
Reviewed against all entries in `references/data/observations.md` and `references/data/experiments.md`.

> **Completion rule:** This file must contain either ≥1 flagged idol with a patch or resolution,
> OR the explicit declaration below. An empty table is a Phase 5 failure.

---

## Declaration (fill in one)

- [ ] Idols detected — see table below
- [ ] **No idols detected in this session.** All assumptions were confirmed by live tool calls.

---

## Idol Flags

| Idol | Flag | Status | Patch or Action |
|---|---|---|---|
| Tribe | "The target API is public and stable" — assumed without probing auth or rate limits | Unresolved | Needs resolution: run auth probe (fetch_url with no token) before build |
| Cave | "A similar pattern worked in a previous skill" — applied without verifying this environment | Patched | Confirmed: ran exec probe, environment differs — old pattern does not apply |
| Marketplace | "This API is standard" — vague label used without defining what standard means here | Patched | Defined in references/taxonomy.md: "standard" means REST + JSON, confirmed by Observation 1 |
| Theatre | "Basecamp/X uses this approach so it must be correct" — argument from authority, no data cited | Unresolved | Removed assumption — no evidence found in observation log to support it |

> ⚠ The rows above are **examples**. Replace them with flags derived from your actual session.
> Delete any row that does not correspond to a real bias observed during Phases 2–3.
> If a bias type was not present, omit that row entirely — do not leave placeholder rows.

---

## Idol Reference

| Idol | What it is | How it typically surfaces |
|---|---|---|
| **Tribe** | Human nature sees patterns that aren't there — shared biases across all people | "This API is well-documented and stable" repeated across blog posts, never verified live |
| **Cave** | Personal bias from your own habits, past experience, or environment | "I used this pattern before so it will work here" |
| **Marketplace** | Words become treated as facts — language confounds thinking | "This is a standard API" — vague label masking an uninvestigated endpoint |
| **Theatre** | Blind belief in systems, authority, or received wisdom | "Stripe/Basecamp/X does it this way so it's correct" — no data cited |

---

## Session Notes

<!-- 
Add any free-form notes about bias signals that didn't fit cleanly into the table.
For example: "Tool returned ambiguous result — unclear if Tribe bias is present or if the API
genuinely behaves inconsistently. Flagged for follow-up in next session."
-->

_No additional notes._

---

*Generated by skill-bacon-creator v1.1 — Bacon's Empiricist Skill Builder*
